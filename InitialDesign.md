# Exit 3 초기 설계 문서

## 1. 프로젝트 개요

플레이어가 반복되는 지하철 출구 공간을 관찰하고 이상현상의 유무를 판단하는 1인칭 게임이다. 같은 공간을 반복해서 보여주되 Stage가 진행될수록 더 세밀한 이상현상을 사용한다.

이 프로젝트는 시각적 디자인보다 게임 진행, 무작위 이상현상, 레벨 초기화, UI와 입력 전환 등 핵심 기능을 안정적으로 구현하는 데 중점을 둔다.

### 개발 및 실행 환경

| 항목 | 내용 |
| --- | --- |
| Unreal Engine | 5.6.1 |
| 주요 구현 방식 | C++ |
| Blueprint | 배치, 에셋 연결, UMG 등 필요한 경우에만 최소 사용 |
| 목표 플랫폼 | Windows 64-bit |
| 목표 해상도 | 1920×1080 |
| 목표 프레임 | 30 FPS |
| 입력 장치 | 키보드, 마우스 |
| 사용 에셋 | 무료 에셋만 사용 |

---

## 2. 핵심 게임 루프

게임은 총 3개의 Stage로 구성한다. 각 Stage에서는 정상 또는 이상 상태가 50% 확률로 결정된다. 이상 상태인 경우 현재 난이도에 속하는 5개 이상현상 중 하나를 무작위로 적용한다.

플레이어는 공간을 관찰한 뒤 진행 방향으로 계속 갈지, 왔던 길로 돌아갈지 행동으로 답을 선택한다.

- 앞으로 계속 진행: 이상현상이 없다고 판단
- 왔던 길로 돌아감: 이상현상이 있다고 판단
- 정답: 다음 Stage로 진행
- 오답: Stage 1부터 다시 시작
- Stage 3 정답: Game Clear

Stage마다 필요한 정답은 1회이며 시간제한은 없다.

```text
Main Menu
    ↓
Stage 1 - 난이도 하
    ↓ 정답
Stage 2 - 난이도 중
    ↓ 정답
Stage 3 - 난이도 상
    ↓ 정답
Game Clear

어느 Stage에서든 오답
    ↓
Stage 1부터 재시작
```

---

## 3. 플레이어

### 시점 및 이동

- 1인칭 시점
- 걷기
- 달리기
- 점프
- 앉기 미지원

### 판정 방식

별도의 선택 버튼을 누르지 않는다. 플레이어가 통과한 트리거와 현재 라운드의 정답을 비교해 자동으로 판정한다.

| 플레이어 행동 | 선택한 답 |
| --- | --- |
| 공간을 통과해 앞으로 계속 진행 | 정상 |
| 관찰 후 왔던 길로 되돌아감 | 이상현상 있음 |

---

## 4. Stage와 이상현상

Stage가 진행될수록 이상현상의 발견 난이도가 높아진다.

| Stage | 난이도 | 특징 | 이상현상 후보 수 |
| --- | --- | --- | ---: |
| Stage 1 | 하 | 누가 봐도 알아볼 수 있는 명확한 변화 | 5개 |
| Stage 2 | 중 | 주변을 의식적으로 살펴보면 발견 가능한 변화 | 5개 |
| Stage 3 | 상 | 정상 상태를 기억하고 세밀하게 관찰해야 발견 가능한 변화 | 5개 |

### 공통 생성 규칙

1. 라운드가 시작되면 정상과 이상 상태 중 하나를 50:50 확률로 결정한다.
2. 정상 상태라면 이상현상을 적용하지 않는다.
3. 이상 상태라면 현재 Stage의 후보 5개 중 하나를 무작위로 선택한다.
4. 한 라운드에는 이상현상을 정확히 1개만 적용한다.
5. 새 게임 진행 맵의 로딩이 끝난 뒤, 플레이어가 진입하기 전에 적용을 완료한다.

### Stage 1 — 난이도 하

처음 플레이하는 사람도 규칙을 이해할 수 있도록 크고 명확한 변화를 사용한다.

1. 벤치 등 큰 오브젝트의 크기가 비정상적으로 커짐
2. 대형 안내판 또는 큰 오브젝트가 사라짐
3. 평소 닫혀 있는 문이 완전히 열림
4. 조명 색상이 강한 붉은색 등으로 변경됨
5. 조명이 계속 깜빡임

### Stage 2 — 난이도 중

주변을 의식적으로 살펴보면 발견할 수 있는 변화를 사용한다.

1. 포스터 이미지 변경
2. 의자 또는 쓰레기통 위치 변경
3. 조명 하나 소등
4. 벤치 또는 중형 오브젝트 방향 변경
5. CCTV 방향 변경

### Stage 3 — 난이도 상

정상 공간의 모습을 기억하고 세세하게 관찰해야 발견할 수 있는 변화를 사용한다.

1. 시계 시간 변경
2. 안내판 숫자 또는 문자 변경
3. 바닥 타일 하나 변경
4. CCTV 방향 미세 변경
5. 작은 오브젝트 하나 제거

NPC는 필수 요소에서 제외한다.

---

## 5. 공간 구성

공간의 콘셉트는 지하철 출구다. 하나의 연결 통로와 하나의 게임 진행 맵을 반복해서 사용한다.

적절한 무료 지하철 에셋을 쉽게 확보할 수 있으면 사용한다. 에셋 확보가 구현 일정을 방해한다면 Unreal Engine의 Cube, Plane, Cylinder, 기본 Material과 조명만으로 공간을 구성한다.

### Persistent Level

게임 내내 유지되는 요소를 배치한다.

- 연결 통로
- 플레이어
- 게임 진행 맵 진입 트리거
- 정상 및 이상 선택 판정 트리거
- Stage와 라운드 진행 관리자
- HUD
- 스트리밍 레벨 관리자

### Gameplay Level

라운드마다 삭제하고 다시 생성하는 공간이다.

- 지하철 출구 관찰 공간
- 이상현상 대상 오브젝트
- 조명
- 포스터
- 안내판
- CCTV
- 문, 벤치, 쓰레기통 및 소형 소품

---

## 6. 맵 반복 및 초기화 방식

게임 진행 맵은 동적 레벨 스트리밍 방식으로 관리한다. C++에서 게임 진행 레벨 인스턴스를 불러오고, 라운드가 종료되면 기존 인스턴스를 제거한 뒤 새 인스턴스를 생성한다.

```text
연결 통로
    ↓
진입 트리거
    ↓
게임 진행 맵
    ↓
이상현상 유무 관찰
    ↓
앞으로 진행 또는 왔던 길로 복귀
    ↓
판정 트리거
    ↓
연결 통로
    ↓
기존 게임 진행 맵 삭제
    ↓
새 게임 진행 맵 생성
    ↓
진입 트리거
    ↓
새 라운드 반복
```

### 라운드 전환 순서

1. 플레이어가 정상 또는 이상 판정 트리거를 통과한다.
2. 현재 라운드의 정답과 플레이어의 선택을 비교한다.
3. 정답이면 다음 Stage, 오답이면 Stage 1로 진행 상태를 변경한다.
4. 현재 Stage UI를 갱신한다.
5. 플레이어가 연결 통로로 이동한다.
6. 기존 게임 진행 맵을 제거한다.
7. 새로운 게임 진행 맵을 생성한다.
8. 정상 또는 이상 상태를 새로 무작위 결정한다.
9. 이상 상태라면 현재 Stage의 이상현상 하나를 적용한다.
10. 게임 진행 맵 준비가 끝난 뒤 플레이어의 진입을 허용한다.

레벨 자체를 다시 생성하여 이전 라운드의 오브젝트 상태가 다음 라운드에 남지 않도록 한다.

---

## 7. 게임 상태 및 판정

핵심 진행 상태는 C++에서 관리한다.

- 현재 Stage
- 현재 라운드가 정상인지 이상인지
- 현재 적용된 이상현상
- 게임 진행 맵의 로딩 상태
- 플레이어가 판정 트리거를 이미 통과했는지 여부

판정은 한 라운드에 한 번만 처리한다. 중복 Overlap이나 트리거 재진입으로 Stage가 여러 번 변경되지 않도록 보호한다.

### 결과 공개

- 별도의 정답 또는 오답 문구를 표시하지 않는다.
- 정답이면 현재 Stage UI가 다음 Stage로 변경된다.
- 오답이면 현재 Stage UI가 Stage 1로 초기화된다.
- Stage 1에서 오답이면 UI는 Stage 1 상태를 유지한다.
- Stage 3에서 정답이면 Game Clear 화면으로 이동한다.

---

## 8. UI

### Main Menu

- 게임 시작
- 조작법
- 설정
- 나가기

### 플레이 HUD

- 현재 Stage 표시

### Pause Menu

플레이 도중 `Esc` 키를 누르면 게임을 일시정지하고 Pause Menu를 표시한다.

- 게임 계속하기
- 설정
- 메인 메뉴로 나가기

### Game Clear

Stage 3을 통과하면 Game Clear 화면을 표시한다.

UI가 열릴 때는 마우스 커서를 표시하고 UI 입력 모드로 전환한다. 게임으로 돌아갈 때는 커서를 숨기고 게임 입력 모드로 복원한다.

---

## 9. 연출

게임이 진행될수록 긴장감을 높이는 방향으로 환경을 변화시킨다. 연출은 핵심 기능 구현 이후 일정에 맞춰 적용한다.

```text
Stage 1
밝은 조명 / 안정적인 환경음

Stage 2
조금 낮아진 조도 / 간헐적인 노이즈

Stage 3
어두운 조명 / 적은 환경음 / 긴장감 있는 사운드
```

이상현상 자체뿐 아니라 공간 분위기도 점차 불안하게 만들어 난이도 상승을 체감하게 한다. 다만 어두운 조명 때문에 이상현상을 식별할 수 없게 만들지는 않는다.

---

## 10. 구현 원칙

### C++ 담당

- 플레이어 이동, 달리기 및 점프
- Stage 진행 및 초기화
- 정상 및 이상 상태 무작위 결정
- 이상현상 선정, 적용 및 관리
- 앞/뒤 트리거 판정
- 동적 레벨 로딩과 제거
- Pause 및 입력 모드 전환
- 핵심 게임 상태와 UI 데이터 관리

### Blueprint 담당

- C++ 클래스를 기반으로 한 에셋 지정용 자식 Blueprint
- 레벨 내 오브젝트와 트리거 배치
- Mesh, Material, Sound 지정
- UMG 레이아웃과 간단한 애니메이션
- 조명 및 환경음 수치 조절

Level Blueprint에는 핵심 게임 로직을 작성하지 않는다.

---

## 11. 제출 및 우선순위

### 제출물

- Git 저장소 링크
- 전체 플레이 과정을 녹화한 시연 영상

### 구현 우선순위

1. 연결 통로와 게임 진행 맵의 반복 구조
2. 동적 레벨 삭제 및 재생성의 안정성
3. 정상 및 이상 상태 무작위 결정
4. 앞/뒤 선택 판정
5. Stage 진행과 오답 초기화
6. 난이도별 이상현상 5개씩, 총 15개
7. Main Menu, HUD, Pause Menu, Game Clear
8. 입력 모드와 마우스 커서 전환
9. Stage별 조명과 사운드 연출
10. 에셋과 UI의 시각적 완성도

프로젝트의 목표는 평가 기준을 형식적으로 모두 구현하는 것이 아니라, 의도한 이상현상 판별 게임을 안정적으로 완성하는 것이다. 점수, 체력, 아이템, 디버프 등 게임 콘셉트에 필요하지 않은 시스템은 필수 범위에 포함하지 않는다.

---

## 12. 최종 게임 흐름

```text
게임 시작
    ↓
Main Menu
    ↓
연결 통로
    ↓
Stage 1 게임 진행 맵
    ↓
Normal 50% / Anomaly 50%
    ↓
공간 관찰
    ↓
앞으로 진행 / 왔던 길로 복귀
    ↓
정답 판정
 ┌──┴──┐
정답    오답
 │       │
 ↓       ↓
Stage 2  Stage 1
 │
 ↓
Stage 3
 │
 ↓
Game Clear

각 판정 후:
연결 통로 → 기존 게임 진행 맵 삭제 → 새 게임 진행 맵 생성
```
